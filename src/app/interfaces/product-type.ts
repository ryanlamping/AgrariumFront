// Using interface to put json response from database into the interface's type of array

/**
 * interface for type of product
 */
export interface ProductType {
  type_name: string,
  type_id: string
}